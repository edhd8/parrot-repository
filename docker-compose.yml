version: '3'

services:
  spring:
    image: edhd8/fuego-de-quasar:1.0
    ports:
    - 8088:8088
    depends_on:
      - postgres
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: 'resistance'
      POSTGRES_USER: 'admin'
      POSTGRES_PASSWORD: 'admin'